<core:FragmentDefinition xmlns:core="sap.ui.core" xmlns="sap.m" xmlns:form="sap.ui.layout.form">
    <VBox>
        <form:SimpleForm title="{user>/user/userName}">
            <form:content>
                <Label text="ID" />
                <Text text="{user>/user/id}" />
                <Label text="userName" />
                <Text text="{user>/user/userName}" />
                <Label text="emails" />
                <Text text="{user>/user/emails/0/value}" />
                <Label text="생성시간" />
                <Text text="{user>/user/meta/created}" />
                <Label text="마지막 업데이트" />
                <Text text="{user>/user/meta/lastModified}" />
            </form:content>
        </form:SimpleForm>
        <List items="{user>/user/groups}" mode="Delete" delete="onDeleteUserRole">
            <headerToolbar>
                <OverflowToolbar>
                    <Text text="역할"/>
                    <ToolbarSpacer/>
                    <Button text="역할 추가" press="onOpenDialog">
                        <customData>
                            <core:CustomData key="RoleDialog" value="addRole"/>
                        </customData>
                    </Button> 
                </OverflowToolbar>
            </headerToolbar>
            <items>
                <StandardListItem title="{user>value}" />
            </items>
        </List>
    </VBox>
</core:FragmentDefinition>