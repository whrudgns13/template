<core:FragmentDefinition xmlns:core="sap.ui.core" xmlns="sap.m" xmlns:f="sap.ui.layout.form">
    <Dialog title="{user>/userState} User">
        <content>
            <f:SimpleForm id="userForm">
                <f:content>
                    <Label text="First Name" labelFor="FirstName" required="true"/>
                    <Input value="{user>/user/name/familyName}" />
                    <Label text="Last Name"  required="true"/>
                    <Input value="{user>/user/name/givenName}"/>
                    <Label text="Email"  required="true"/>
                    <Input value="{user>/user/emails/0/value}" type="Email" valueStateText="이메일 형식이 아닙니다"/>
                </f:content>
            </f:SimpleForm>
            <!-- <f:Form id="userForm">
                <f:layout>
                    <f:ResponsiveGridLayout
                        labelSpanXL="4"
                        labelSpanL="4"
                        labelSpanM="4"
                        labelSpanS="12"
                        adjustLabelSpan="false"
                        emptySpanXL="0"
                        emptySpanL="0"
                        emptySpanM="0"
                        emptySpanS="0"
                        columnsXL="12"
                        columnsL="12"
                        columnsM="12"
                        singleContainerFullSize="false" />
                </f:layout>
                <f:formContainers>
                    <f:FormContainer>
                        <f:formElements>
                            <f:FormElement label="First Name" required="true">
                                <f:fields>
                                    <Input value="{user>/selectedUser/name/familyName}" visible="{= ${user>/userState}==='edit' ? true : false}" />
                                    <Input value="{user>/createUser/name/familyName}" visible="{= ${user>/userState}==='create' ? true : false}" />
                                </f:fields>
                            </f:FormElement>
                            <f:FormElement label="Last Name" required="true">
                                <f:fields>
                                    <Input value="{user>/selectedUser/name/givenName}" visible="{= ${user>/userState}==='edit' ? true : false}" />
                                    <Input value="{user>/createUser/name/givenName}" visible="{= ${user>/userState}==='create' ? true : false}" />
                                </f:fields>
                            </f:FormElement>
                            <f:FormElement label="Email" required="true">
                                <f:fields>
                                    <Input value="{user>/selectedUser/emails/0/value}" visible="{= ${user>/userState}==='edit' ? true : false}" type="Email" valueStateText="이메일 형식이 아닙니다" />
                                    <Input value="{user>/createUser/emails/0/value}" visible="{= ${user>/userState}==='create' ? true : false}" type="Email" valueStateText="이메일 형식이 아닙니다" />
                                </f:fields>
                            </f:FormElement>
                        </f:formElements>
                    </f:FormContainer>
                </f:formContainers>
            </f:Form> -->
        </content>
        <beginButton>
            <Button text="Submit" type="Emphasized" press="onSubmit" />
        </beginButton>
        <endButton>
            <Button text="Cancel" press="onClose" />
        </endButton>
    </Dialog>
</core:FragmentDefinition>